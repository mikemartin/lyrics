const { colors } = require('tailwindcss/defaultTheme')

module.exports = {
  theme: {
    colors: {
      'primary': colors.pink,
      'secondary': colors.teal
    },
    fontFamily: {
      display: [
        'Kalam', 
        'cursive'
      ]
    },
  }
}


