const { colors } = require('tailwindcss/defaultTheme')

module.exports = {
  theme: {
    colors: {
      'primary': colors.pink,
      'secondary': colors.black
    },
    fontFamily: {
      display: [
        'Kalam', 
        'cursive'
      ]
    },
  }
}


